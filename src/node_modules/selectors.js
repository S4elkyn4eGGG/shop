const R = require('ramda');

const getPhoneById = (state, id) => R.prop(id, state.phones);

export const getPhones = state => {
    if(state.phonesPage.ids) {
        return R.map(id => getPhoneById(state, id), state.phonesPage.ids);
    }
};